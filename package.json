{
  "name": "nuxt-jest-cypress-directus-storybook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "npm run build:all",
    "build:all": "turbo run build --parallel",
    "test": "turbo run test --color --output-logs=new-only",
    "format": "turbo run format"
  },
  "workspaces": [
    "apps/**",
    "packages/**"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "turbo": "^1.6.3",
    "vue-server-renderer": "^2.7.14"
  }
}
