{
  "name": "functions",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3010",
    "open:dev": "vite --port 3010 --open",
    "start": "vite serve",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 0",
    "open": "open http://localhost:3010 || start http://localhost:3010 || xdg-open http://localhost:3010"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.0.0"
  }
}